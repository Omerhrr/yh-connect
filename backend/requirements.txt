fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
pydantic==2.10.4
pydantic-settings==2.7.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# passlib 1.7.4 (last released 2020) is incompatible with bcrypt>=4.1, which
# changed its 72-byte-password handling to raise instead of silently
# truncating. That breaks passlib's internal self-test the first time it's
# used, with "password cannot be longer than 72 bytes" - on every hash/verify
# call, i.e. every login and registration. Pin bcrypt explicitly so a fresh
# install (like Render's) doesn't resolve to an incompatible version.
bcrypt==4.0.1
python-multipart==0.0.20
psycopg2-binary==2.9.10
alembic==1.14.0
email-validator==2.2.0
httpx==0.28.1
slowapi==0.1.9
pytest==8.3.4
reportlab==4.2.5
